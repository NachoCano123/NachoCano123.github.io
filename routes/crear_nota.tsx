import {FormNota} from "../components/FormNota.tsx";
import Layout from "../components/Layout.tsx";

export default function CrearNota() {
  return (
    <Layout>
      <FormNota />
    </Layout>
  );
}